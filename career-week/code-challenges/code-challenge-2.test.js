const { revenue } = require('./code-challenge-2');

describe('revenue function', () => {
  it('takes in an array of transactions and returns an object of the daily revenue', () => {
    const 
  })

});

//convert the timestamp to a date string: 'weekday month date year'
  //pay attention to timezone!

//add function to add all of the price values for each date

